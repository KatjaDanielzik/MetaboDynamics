# here should be the code stored to convert raw data into data that is
# available for the user

# this is only dummy code!!!! to enable practising of data documentation
process <- function(a,b,c){
  temp <- a+b+c
  return(temp)
}
